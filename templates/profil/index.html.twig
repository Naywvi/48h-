{% extends 'base.html.twig' %}

{% block title %}Hello ProfilController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <h1>Hello {{ controller_name }}! ✅</h1>

    This friendly message is coming from:
    <ul>
        {# <li>Your controller at <code><a href="{{ 'C:/Users/duane/Documents/Travail VS Code/Challenge48h/48h-/src/Controller/ProfilController.php'|file_link(0) }}">src/Controller/ProfilController.php</a></code></li>
        <li>Your template at <code><a href="{{ 'C:/Users/duane/Documents/Travail VS Code/Challenge48h/48h-/templates/profil/index.html.twig'|file_link(0) }}">templates/profil/index.html.twig</a></code></li> #}
    </ul>
</div>
	<section class="row product">
	<section class="row profil">
		{% for profils in profil %}
				<profil class="col-md-4 mx-auto text-center border border-dark mb-3 p-3 mt-3 mr-1">
                <h2>Username : {{ profil.username }}</h2>
				<div class="profil">
				<br>
					<img src="{{ profil.image }}" class="img-fluid">
					<br>
					<div>Email : {{ profil.email }}</div>
					<br>
					<div>
					<div>Vos commentaire : {{ profil.commentaire_id | raw }}</div>
					</div>
                    <div>
					<div>Vos evenement : {{ profil.evenement_id | raw }}</div>
					</div>
					<a href="{{ path('panier_add', {'id' : products.id}) }}" class="btn btn-primary mt-2">Ajouter au panier</a>
				</div>
			</product>
		{% endfor %}
	</section>
{% endblock %}
